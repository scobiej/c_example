#include <stdlib.h>

#include "list.h"

int main() {
   insert(10);
   insert(20);
   insert(30);
   insert(1);
   insert(40);
   insert(56); 
   insert(66); 
   insert(104); 

   printList();

   return EXIT_SUCCESS;
}
